1) main target - "to do list"
2) task source = https://www.hackampus.com/student/assignment/5192
3) https://jsonplaceholder.typicode.com/todos?_limit=5
4) https://github.com/3831991/W140925ER

Implementation:
    0) Main windows design

    1) Basic functionality for single task:
        1.1) add
        1.2) delete
        1.3) edit

    2) Filtering by
        2.1) id
        2.2) select
        2.3) status
        2.4) group   
        2.5) priority
        2.6) details 
        2.7) deadline
        2.8) created
        2.9) updated

    3) Sorting (once clicking on appropriate table header) ASC and DESC
        3.1) by id
        3.2) by status
        3.3) by group name
        3.4) by deadlines
        3.5) by creation date
        3.6) by priority
    
    4) Accessability

    5) Responsivity

    6) Export button functionality:
        On click new buttons appears to allow
            - Export to localstorage & replace localstorage 
            - Export to localstorage & add to localstorage
            - Export to local disk

    7) Import button functionality:
        On click new buttons appears to allow
            - Import from localstorage & replace current 
            - Import from localstorage & add to current
            - Import from local disk

    8) History of each task changes

//***********************************************************************************

1) Statistics for tasks
    1.1) On click - Open a new modal where will be options for selection:
        Total tasks = num of all tasks
        Done = num of done tasks
        Priority 
            0 = ...
            1 = ...
            
            10 = ...
        Groups =  count of groups
        Latest deadline = date of latest deadline
        Closest deadline = date of closest deadline
        Mostly changed = @id of task with longest history

2) Responsivity-additional:
    for mob screen:
    - replace "Upload" with "U" and arrow up
    - replace "Download" with "D" and arrow down
    - move (+XXX UTC) to the header of deadline

3) Tasks group actions:
    2.1) Adding batch of tasks (all the accessable fields)
    2.2) Editing batch of tasks 
        2.2.1) deadlines
        2.2.2) groups
    2.3) Deletion all the tasks related to the group

4) Documentation

5) Optimization

6) Actions with confirmation
    6.1) Deletion
    6.2) Editing
